---
language: node_js
sudo: false
node_js:
  - "0.11"
  - "0.10"

env:
  - TEST_DIR=packages/frontend
  - TEST_DIR=packages/core
  - TEST_DIR=packages/checkouts
  - TEST_DIR=packages/auth

before_install:
  - cd $TEST_DIR

install:
  - npm install -g bower
  - npm install
  - bower install

script:
  - ember test
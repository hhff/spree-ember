<div class="row">
  {{#each products as |product|}}
    {{#link-to 'spree.products.show' product}}
      <div class="small-12 medium-6 columns text-center">
        <img style="height:100px" src="{{product.images.firstObject.largeUrl}}">
        <h6>{{product.name}}</h6>
        <hr>
        <h6>{{product.displayPrice}}</h6>
      </div>
    {{/link-to}}
  {{/each}}
</div>
